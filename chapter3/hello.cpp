#include <iostream> // tell the compiler to include standard io stuff

int main() { // main func, takes no args, does nothing.
    std::cout << "hello world!\n"; // standard c out, put the string there
    // if no problems are encountered the main func automatically returns 0
}
